<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <item name="android:tabWidgetStyle">@style/ActionBarTabStyle</item>
    </style>

    <style name="ActionBarTabStyle" parent="AppTheme">
        <item name="android:background">@drawable/tab_indicator_custom</item>
        <item name="android:gravity">center</item>
    </style>

</resources>
